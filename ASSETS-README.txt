The supplied login HTML references these image filenames:
ashoka_piller.png
nhm.png

Place those existing image files in this folder before deployment. They were referenced by the uploaded login page but were not included as binary uploads here.
